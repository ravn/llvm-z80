# Handoff: end of session 36 → start of session 37

**Date:** 2026-05-02 (end of session 36).
**Read this first** at the start of session 37 (or any future
session opening this workspace).

## In one paragraph

Session 36 was a planning + research + sync session.  No codegen
changes; the master plan `tasks/roadmap-to-maturity.md` was written,
upstream `llvm-z80/llvm-z80:main` was merged in cleanly (sizes
byte-exact, lit 77 PASS + 1 XFAIL), and one trivial issue
(ravn/llvm-z80#101 — missing `override` markers post-merge) was
filed.  Phase 1 (Foundation) of the roadmap is the next active
phase.  Working mode is **workspace mode** — no engagement with
`llvm-z80/llvm-z80` (the upstream fork parent) until a substantial
body of work is ready.

## Repository state at session 36 close

```
ravn/llvm-z80:main              983266a6  Merge branch 'session-36-code-density-plan'
ravn/llvm-z80:session-36-code-density-plan  d733b91f  tasks: session 36 summary
upstream:llvm-z80/llvm-z80:main  a016d5bc  [Z80] Fix build after Combiner API change
```

Both `main` and `session-36-code-density-plan` pushed.  Working
tree clean.

## What changed (session 36)

  - **Plan documents added** (under `llvm-z80/tasks/`):
    - `roadmap-to-maturity.md` — master plan (872 lines).
    - `session36-summary.md` — milestone record.
    - `phase-a-findings.md` — null-result diagnostic (volatile
      forces the load-after-store reload; not optimization
      target).
    - `phase-c-regalloc-investigation.md` — Cluster A (regalloc)
      design notes; CORRECTED later by peer-target research.
    - `backend-completion-roadmap.md` — 16-area audit lens
      (complement to fix-plan.md).
    - `plan-code-density-2026-05-02.md` — superseded false start.

  - **Plan documents updated**:
    - `fix-plan.md` — session-36 update + Cluster 8 (regalloc)
      added.

  - **Memories saved or revised** (`~/.claude/projects/-Users-ravn-z80/memory/`):
    - `project_z80_backend_unfinished.md` — backend is preliminary.
    - `project_z80_upstream_goal.md` — staged collaboration model
      (workspace mode → engagement mode split).
    - `project_hitech_third_compiler.md` — TODO: add ravn/hitech.
    - `feedback_root_cause_over_peephole.md` — favor upstream fixes.
    - `feedback_no_compliments.md` — added "Good catch" to forbidden.

  - **Upstream merged**: `llvm-z80/llvm-z80:main` → ravn `main`.
    1 Z80-source change (Combiner API fix); rest is LLVM upstream
    tracking.

  - **Issue filed**: `ravn/llvm-z80#101` (override markers post-merge).

## Next-session entry points (in priority order)

### Option A: Phase 1 (Foundation) — broad

Per `roadmap-to-maturity.md` §15 and §12.1.  Three concurrent
sub-tasks:

  1. **Late-opt audit** — read `Z80LateOptimization.cpp` end-to-end,
     classify each peephole as **keep** / **migrate upstream** /
     **delete**.  Reference: `tasks/peephole-vs-root-cause.md`.
     Output: `tasks/late-opt-audit.md`.
  2. **CI workflow YAML** — GitHub Actions for the Z80 lit suite.
     Workspace testbed only (do not propose to llvm-z80/llvm-z80
     yet).  Reference: `tasks/fix-plan.md` "Infrastructure
     follow-ups" section.
  3. **Per-function size baseline tracker** — script under
     `llvm-z80/tools/` or `rcbios-in-c/tools/` that runs
     `llvm-nm --print-size --size-sort` and reports diffs against
     a committed baseline CSV.

### Option B: Issue #101 — narrow warm-up

The smallest concrete fix in the queue.  Add `override` to
`Z80TargetTransformInfo.h:614` (`getNumberOfRegisters`) and
`:74` (`getRegisterBitWidth`).  Build + verify warnings gone.
Lit test if possible (probably not — this is purely a build-
warning fix).

### Option C: Issue #81 — first correctness fix

MC parser rejects `ex af, af'` because `'` is treated as a string
quote.  Smallest of the 5 correctness bugs.  Recommended starting
move per `roadmap-to-maturity.md` §12.2.

## Suggested branch for session 37

Based on which entry point above:

  - Option A: `session-37-phase-1-foundation` (broad)
  - Option B: `session-37-issue-101-override` (narrow warm-up)
  - Option C: `session-37-issue-81-asm-apostrophe` (correctness)

The pre-created branch (see Section "Pre-created branch" below)
is `session-37-phase-1-foundation` since it's the most flexible
starting point.  Rename or branch off it freely.

## Critical context to keep in mind

### From memories (load automatically via MEMORY.md)

  - **`project_z80_backend_unfinished.md`**: backend is preliminary;
    goal is to FINISH it correctly, not optimize a finished one.
  - **`project_z80_upstream_goal.md`**: staged collaboration with
    `llvm-z80/llvm-z80` first (workspace mode → engagement mode
    gate); official LLVM upstream is long-term aspiration.
  - **`feedback_root_cause_over_peephole.md`**: prefer upstream
    fixes (TableGen, GISel combiner, regalloc cost model, target
    hooks) over post-RA peepholes for new work.
  - **`feedback_no_upstream_issues.md`**: file issues only in
    ravn/* forks, never in llvm/llvm-project.
  - **`feedback_no_pull_requests.md`**: HARD RULE — never `gh pr
    create` unless the user explicitly asks in the current turn.

### From plan docs

  - **Workspace mode** (current): all work on `ravn/llvm-z80`,
    issue tracking on `ravn/llvm-z80`, no `llvm-z80/llvm-z80`
    issues/PRs/comments until *substantial* (per
    `roadmap-to-maturity.md` §10.2).
  - **Quality bar**: LLVM coding standards, lit tests on `*.ll`
    IR fixtures (no BIOS/cpnos-rom dependence in upstream-bound
    tests), clang-format clean, license headers.
  - **Reverting prior fork-only work is acceptable** when it
    wouldn't survive upstream review.
  - **DJNZ is NOT to become a primary opcode**; the right design
    is DJNZ_LOOP pseudo + register hint + existing peephole (per
    peer-target precedent research).

## Verification baseline at session 36 close

| Check | Value |
| --- | --- |
| `ravn/llvm-z80:main` HEAD | `983266a6` |
| Build (`ninja clang llc`) | clean (2 warnings → #101) |
| Z80 lit suite | 77 PASS + 1 XFAIL (#99) |
| rcbios BIOS size | 5920 bytes |
| cpnos-rom payload | 1708 bytes |
| cpnos PROM0 non-padding | 1777 bytes |
| Open issues (ravn/llvm-z80) | 28 (was 27 + #101) |

If session 37 starts and any of these diverge unexpectedly,
something has gone wrong — investigate before adding new work.

## How to build

Per `llvm-z80/CLAUDE.md`:

```sh
# Native macOS build (preferred)
export PATH="/Applications/CLion.app/Contents/bin/ninja/mac/aarch64:/Applications/CLion.app/Contents/bin/cmake/mac/aarch64/bin:$PATH"
cd /Users/ravn/z80/llvm-z80
ninja -C build-macos clang llc FileCheck not count llvm-config

# Z80 lit
build-macos/bin/llvm-lit -j4 llvm/test/CodeGen/Z80/

# rcbios + cpnos-rom (real-workload sanity)
cd /Users/ravn/z80/rc700-gensmedet/rcbios-in-c
rm -f builddate.h builddate.inc && make clang-bios   # expects "clang BIOS: 5920 bytes"

cd /Users/ravn/z80/rc700-gensmedet/cpnos-rom
rm -f cpnos_buildinfo.h && rm -rf clang && make cpnos
# expects "payload:  1708 bytes"
```

## Pre-created branch

`session-37-phase-1-foundation` is created at the same commit as
`main` (`983266a6`).  Switch to it on session 37 start:

```sh
cd /Users/ravn/z80/llvm-z80
git checkout session-37-phase-1-foundation
```

If session 37 wants a different starting topic (Option B or C
above), branch off main as `session-37-<topic>` and ignore the
pre-created branch (or delete it).

## What is NOT in this handoff

  - Specific code changes to make.  The plan in
    `roadmap-to-maturity.md` describes them at the cluster level;
    the per-issue work happens in session 37+.
  - Communication with @zlfn.  Per workspace-mode rule, none until
    engagement mode opens.
  - Pushes of session-37 branches to origin — done by the session
    when appropriate.

## End-of-handoff checklist

  - [x] Master plan committed and pushed
    (`tasks/roadmap-to-maturity.md`).
  - [x] Session 36 summary committed
    (`tasks/session36-summary.md`).
  - [x] Memories updated (5 new/revised).
  - [x] CLAUDE.md (project root) reflects session 36 state and
    points to the maturity roadmap.
  - [x] Upstream merge pushed; both branches on origin.
  - [x] Issue #101 filed in `ravn/llvm-z80`.
  - [x] Pre-created branch `session-37-phase-1-foundation` ready.
  - [x] This handoff document written.

End of session 36 handoff.
