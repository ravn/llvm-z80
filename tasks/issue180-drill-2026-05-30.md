# #180 peephole-audit re-audit — 2026-05-30

Re-audit of the #180 tracker (Z80LateOptimization.cpp; the 2026-05-02 audit
`tasks/late-opt-audit-2026-05-02.md` classified **16 of 38** peepholes as
"Migrate" / ~2300 LOC of stand-in code for missing upstream infrastructure).

## Movability criterion

A peephole can move to a GISel combiner or ISel TableGen pattern **only if it
does not depend on post-RA information** — final physreg assignment, physreg
liveness, or FLAGS liveness.  Combiners and ISel run pre-RA, where that
information does not exist.

## Finding 1 — the audit is ~half stale: 7 of 16 already gone

Grepping the current source for retired/removed markers, these audit "Migrate"
entries are **already removed or migrated** (sessions 73q / 73s, plus #6 to
ISel):

| # | peephole | status |
|---|---|---|
| 2 | POP rr; PUSH rr elimination | removed (73s) |
| 6 | XOR #0xFF → CPL | **migrated to ISel** (flags-dead form emitted directly) |
| 9 | OR A; LD r,0; JR Z | removed |
| 11 | ALU #imm; ALU #imm idempotent | removed |
| 15 | 16-bit increment overflow-test idiom | removed (73q) |
| 23 | HL save-via-BC roundtrip (#84) | removed |
| 24 | BC ping-pong self-loop (#97) | removed |

So the "16 migrate" is really ~9 still-present.

## Finding 2 — most of the remaining ~9 are post-RA-gated → KEEP, not migrate

Applying the criterion to the still-present audit "Migrate" entries:

| # | peephole | guard | verdict |
|---|---|---|---|
| 7  | LD A,#0 → XOR A | FLAGS-dead (`isRegDeadAfter`) | **Keep** (XOR A clobbers flags) |
| 13 | LD L,H; LD H,0; LD A,L → LD A,H | "H and L dead after" — its own comment already says **Keep** | **Keep** |
| 20 | redundant LD A,reg removal (#60) | physreg pattern + liveness | **Keep** |
| 12 | LD r,A; LD A,r2; ALU r → ALU r2 | commutative + physreg/regalloc | Keep / regalloc |
| 18 | comparison reversal | flags + regalloc hints | Keep / regalloc |
| 21 | known-immediate A tracking (#60/#83) | physreg-A constant tracking, flag-aware | Keep / mixed |

Genuine pure-pattern combiner/ISel candidates that remain (no post-RA dep):
**#8** (A-via-(HL) → direct `LD r,(HL)` — an ISel addressing pattern, cf. the
2026-05-30 #27 indexed-addr work), **#10** (`LD rr,nn; INC → LD rr,nn±1`,
constant fold), **#17** (in-memory INC/DEC), **#19** (`LD (sym),A` reorder),
**#25** (u8 switch range-check → IR switch lowering).  ~3–5 real migrations.

## Conclusion

The "16 migrate / ~2300 LOC stand-in" headline is **substantially overstated**:
- 7 already removed/migrated;
- ~5 of the remainder are post-RA-liveness/regalloc-gated and correctly **Keep**
  (they encode Z80 flag/register semantics the pre-RA layers cannot express —
  #13's comment already self-corrected to Keep);
- only **~3–5** are genuine pure-pattern combiner/ISel candidates.

The file grew 6234 → 6349 LOC since the audit *despite* the 7 removals, because
new legitimately-Z80-ISA peepholes were added (i16 EQ/NE via SBC HL, SM83 HL+/
HL- post-inc loads, SET/RES n,(HL), etc.) — these are "Keep", not stand-ins.

**Net for the upstream-submission gate (#180): the stand-in concern is largely
resolved already.** Realistic remaining migrate work is ~3–5 pure-pattern
peepholes (~600 LOC), each a focused combiner/ISel + MIR-test session mirroring
the #6 CPL migration — not the ~2300 LOC / 6–10 sessions the tracker implies.

## Next step

Confirm the ~3–5 genuine candidates per-peephole and migrate #8/#10/#13-style
pure patterns; reclassify the post-RA-gated ones to Keep in the audit table so
the tracker reflects reality.
