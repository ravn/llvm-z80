[package]
name = "elf2rel"
version = "0.1.0"
edition = "2024"

[dependencies]
object = { version = "0.36", default-features = false, features = ["read_core", "elf"] }

[dev-dependencies]
object = { version = "0.36", default-features = false, features = ["write", "elf"] }
